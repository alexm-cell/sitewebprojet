git clone https://github.com/alexm-cell/sitewebprojet
 ls

 cd sitewebprojet
 ls
 git branch


 git status
 
 git checkout -b css-styles

 nano styles.css
git add styles.css
git commit -m "Fichier CSS"
git config --global user.email "robisoa.corp@gmail.com"
git config --global user.name "Hary94230"

git add styles.css
git commit -m "Fichier CSS"

git push origin css-styles

git checkout main
git merge css-styles
git push origin main
